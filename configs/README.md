# Configuration

This directory contains small, versioned teacher and workflow configuration.
Configs must be portable: use repository-relative paths, environment variables,
or CLI inputs instead of machine-specific absolute paths.

Checkpoint hashes and release manifests belong under `docs/release/` or
`docs/preservation/`. Secrets and local credentials never belong here.
